In the src, create the "redux" folder

https://redux-toolkit.js.org/
In the terminal, install redux tollkit

```
npm install @reduxjs/toolkit
npm install react-redux
```

The first thing we need to do is create a store. 